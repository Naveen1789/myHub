package chapter8;

import java.util.ArrayList;

public class Three_AllSubsetsOfASet {
	
	public static ArrayList<ArrayList<Integer>> getAllSubsets(ArrayList<Integer> set, Integer n){
		
		if(n == null){
			ArrayList<Integer> tempSet = new ArrayList<Integer>();
			tempSet.addAll(set);
			Integer temp = tempSet.remove((tempSet.size() - 1));
			return getAllSubsets();
		}
		
		for(int i = 0; i <= set.size(); i++){
			if(i == 0){
				System.out.println("[]");
			}
			for(){
				
			}
		}
		
		if(set.size() == 0){
			return null;
		}
		
		if(set.size() == 1){
			System.out.println(set.get(0));
			return set;
		}
		for(int i = 0 ; i < set.size() ; i++){
			
		}
		return null;
	}

	public static void main(String[] args) {
		// TODO Auto-generated method stub
		
		ArrayList<Integer> aList = new ArrayList<Integer>();
		aList.add(1);
		aList.add(2);
		aList.add(3);
		aList.add(4);

	}

}
